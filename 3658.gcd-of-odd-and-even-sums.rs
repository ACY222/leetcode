// @leet start
impl Solution {
    pub fn gcd_of_odd_even_sums(n: i32) -> i32 {
        // let sum_odd = n * n;
        // let sum_even = (1 + n) * n;
        //
        // // gcd(n * n, n(n + 1))
        // // = n * gcd(n, n + 1) = n * 1
        // gcd(sum_odd, sum_even)
        n
    }
}
// @leet end
